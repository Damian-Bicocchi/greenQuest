<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Cambiado el parent a Theme.AppCompat para ser compatible con AppCompatActivity -->
    <style name="Theme.GreenQuest" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:backdropColor">@color/fondo_primario</item>
        <item name="android:textColor">@color/texto_normal</item>
        <item name="android:textColorHint">@color/texto_normal</item>
        <item name="android:hint"></item>

    </style>
</resources>