<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <!-- CONTENEDOR DEL DIALOG -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layoutDialogo"
        android:layout_width="302dp"
        android:layout_height="475dp"
        android:background="@drawable/radius_color_secundario"
        android:padding="14dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- PREVIEW FOTO (MARCO) -->
        <FrameLayout
            android:id="@+id/marcoPreview"
            android:layout_width="0dp"
            android:layout_height="160dp"
            android:background="@drawable/marco_para_elementos"
            android:padding="8dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ImageView
                android:id="@+id/imagenActualCambiarImagen"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Foto seleccionada"
                tools:srcCompat="@tools:sample/avatars" />
        </FrameLayout>

        <!-- TEXTO INDICATIVO -->

        <!-- LISTA DE OPCIONES (MARCO) -->
        <FrameLayout
            android:id="@+id/marcoRecycler"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/marco_para_elementos"
            android:padding="8dp"
            app:layout_constraintBottom_toTopOf="@+id/buttonGuardar"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/marcoPreview">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerViewDialogo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:overScrollMode="never"
                tools:listitem="@android:layout/simple_list_item_1" />
        </FrameLayout>

        <Button
            android:id="@+id/buttonGuardar"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_marginEnd="8dp"
            android:text="Guardar"
            android:textSize="14sp"
            android:backgroundTint="#A6DA95"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/buttonCancelar"
            app:layout_constraintWidth_percent="0.48"/>

        <!-- BOTÓN CANCELAR (ID ORIGINAL) -->
        <Button
            android:id="@+id/buttonCancelar"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:text="Cancelar"
            android:textSize="14sp"
            android:backgroundTint="#F49C9C"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/buttonGuardar"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintWidth_percent="0.48"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
